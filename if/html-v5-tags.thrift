
/** 
 * The HtmlTags enum represents HTML tag names as integers so that
 * XPaths can be a list of ints rather than bloated strings.
 */
enum HtmlTags {
    comment    = 0,      // <!--...comment...--> Defines a comment
    DOCTYPE    = 1,      // <!DOCTYPE> Defines the document type
    a          = 2,      // <a> Defines a hyperlink
    abbr       = 3,      // <abbr> Defines an abbreviation or an acronym
    acronym    = 4,      // <acronym> Not supported in HTML5. Use <abbr> instead. Defines an acronym
    address    = 5,      // <address> Defines contact information for the author/owner of a document
    applet     = 6,      // <applet> Not supported in HTML5. Use <embed> or <object> instead. Defines an embedded applet
    area       = 7,      // <area> Defines an area inside an image-map
    article    = 8,      // <article> Defines an article
    aside      = 9,      // <aside> Defines content aside from the page content
    audio      = 10,     // <audio> Defines sound content
    b          = 11,     // <b> Defines bold text
    base       = 12,     // <base> Specifies the base URL/target for all relative URLs in a document
    basefont   = 13,     // <basefont> Not supported in HTML5. Use CSS instead. Specifies a default color, size, and font for all text in a document
    bdi        = 14,     // <bdi> Isolates a part of text that might be formatted in a different direction from other text outside it
    bdo        = 15,     // <bdo> Overrides the current text direction
    big        = 16,     // <big> Not supported in HTML5. Use CSS instead. Defines big text
    blockquote = 17,     // <blockquote> Defines a section that is quoted from another source
    body       = 18,     // <body> Defines the document's body
    br         = 19,     // <br> Defines a single line break
    button     = 20,     // <button> Defines a clickable button
    canvas     = 21,     // <canvas> Used to draw graphics, on the fly, via scripting (usually JavaScript)
    caption    = 22,     // <caption> Defines a table caption
    center     = 23,     // <center> Not supported in HTML5. Use CSS instead. Defines centered text
    cite       = 24,     // <cite> Defines the title of a work
    code       = 25,     // <code> Defines a piece of computer code
    col        = 26,     // <col> Specifies column properties for each column within a <colgroup> element 
    colgroup   = 27,     // <colgroup> Specifies a group of one or more columns in a table for formatting
    datalist   = 28,     // <datalist> Specifies a list of pre-defined options for input controls
    dd         = 29,     // <dd> Defines a description/value of a term in a description list
    del        = 30,     // <del> Defines text that has been deleted from a document
    details    = 31,     // <details> Defines additional details that the user can view or hide
    dfn        = 32,     // <dfn> Represents the defining instance of a term
    dialog     = 33,     // <dialog> Defines a dialog box or window
    dir        = 34,     // <dir> Not supported in HTML5. Use <ul> instead. Defines a directory list
    div        = 35,     // <div> Defines a section in a document
    dl         = 36,     // <dl> Defines a description list
    dt         = 37,     // <dt> Defines a term/name in a description list
    em         = 38,     // <em> Defines emphasized text 
    embed      = 39,     // <embed> Defines a container for an external (non-HTML) application
    fieldset   = 40,     // <fieldset> Groups related elements in a form
    figcaption = 41,     // <figcaption> Defines a caption for a <figure> element
    figure     = 42,     // <figure> Specifies self-contained content
    font       = 43,     // <font> Not supported in HTML5. Use CSS instead. Defines font, color, and size for text
    footer     = 44,     // <footer> Defines a footer for a document or section
    form       = 45,     // <form> Defines an HTML form for user input
    frame      = 46,     // <frame> Not supported in HTML5. Defines a window (a frame) in a frameset
    frameset   = 47,     // <frameset> Not supported in HTML5. Defines a set of frames
    h          = 48,     // <h1> to <h6>  Defines HTML headings
    head       = 49,     // <head> Defines information about the document
    header     = 50,     // <header> Defines a header for a document or section
    hr         = 51,     // <hr> Defines a thematic change in the content
    html       = 52,     // <html> Defines the root of an HTML document
    i          = 53,     // <i> Defines a part of text in an alternate voice or mood
    iframe     = 54,     // <iframe> Defines an inline frame
    img        = 55,     // <img> Defines an image
    input      = 56,     // <input> Defines an input control
    ins        = 57,     // <ins> Defines a text that has been inserted into a document
    kbd        = 58,     // <kbd> Defines keyboard input
    keygen     = 59,     // <keygen> Defines a key-pair generator field (for forms)
    label      = 60,     // <label> Defines a label for an <input> element
    legend     = 61,     // <legend> Defines a caption for a <fieldset> element
    li         = 62,     // <li> Defines a list item
    link       = 63,     // <link> Defines the relationship between a document and an external resource (most used to link to style sheets)
    main       = 64,     // <main> Specifies the main content of a document
    map        = 65,     // <map> Defines a client-side image-map
    mark       = 66,     // <mark> Defines marked/highlighted text
    menu       = 67,     // <menu> Defines a list/menu of commands
    menuitem   = 68,     // <menuitem> Defines a command/menu item that the user can invoke from a popup menu
    meta       = 69,     // <meta> Defines metadata about an HTML document
    meter      = 70,     // <meter> Defines a scalar measurement within a known range (a gauge)
    nav        = 71,     // <nav> Defines navigation links
    noframes   = 72,     // <noframes> Not supported in HTML5. Defines an alternate content for users that do not support frames
    noscript   = 73,     // <noscript> Defines an alternate content for users that do not support client-side scripts
    object     = 74,     // <object> Defines an embedded object
    ol         = 75,     // <ol> Defines an ordered list
    optgroup   = 76,     // <optgroup> Defines a group of related options in a drop-down list
    option     = 77,     // <option> Defines an option in a drop-down list
    output     = 78,     // <output> Defines the result of a calculation
    p          = 79,     // <p> Defines a paragraph
    param      = 80,     // <param> Defines a parameter for an object
    pre        = 81,     // <pre> Defines preformatted text
    progress   = 82,     // <progress> Represents the progress of a task
    q          = 83,     // <q> Defines a short quotation
    rp         = 84,     // <rp> Defines what to show in browsers that do not support ruby annotations
    rt         = 85,     // <rt> Defines an explanation/pronunciation of characters (for East Asian typography)
    ruby       = 86,     // <ruby> Defines a ruby annotation (for East Asian typography)
    s          = 87,     // <s> Defines text that is no longer correct
    samp       = 88,     // <samp> Defines sample output from a computer program
    script     = 89,     // <script> Defines a client-side script
    section    = 90,     // <section> Defines a section in a document
    select     = 91,     // <select> Defines a drop-down list
    small      = 92,     // <small> Defines smaller text
    source     = 93,     // <source> Defines multiple media resources for media elements (<video> and <audio>)
    span       = 94,     // <span> Defines a section in a document
    strike     = 95,     // <strike> Not supported in HTML5. Use <del> or <s> instead. Defines strikethrough text
    strong     = 96,     // <strong> Defines important text
    style      = 97,     // <style> Defines style information for a document
    sub        = 98,     // <sub> Defines subscripted text
    summary    = 99,     // <summary> Defines a visible heading for a <details> element
    sup        = 100,    // <sup> Defines superscripted text
    table      = 101,    // <table> Defines a table
    tbody      = 102,    // <tbody> Groups the body content in a table
    td         = 103,    // <td> Defines a cell in a table
    textarea   = 104,    // <textarea> Defines a multiline input control (text area)
    tfoot      = 105,    // <tfoot> Groups the footer content in a table
    th         = 106,    // <th> Defines a header cell in a table
    thead      = 107,    // <thead> Groups the header content in a table
    time       = 108,    // <time> Defines a date/time
    title      = 109,    // <title> Defines a title for the document
    tr         = 110,    // <tr> Defines a row in a table
    track      = 111,    // <track> Defines text tracks for media elements (<video> and <audio>)
    tt         = 112,    // <tt> Not supported in HTML5. Use CSS instead. Defines teletype text
    u          = 113,    // <u> Defines text that should be stylistically different from normal text
    ul         = 114,    // <ul> Defines an unordered list
    var        = 115,    // <var> Defines a variable
    video      = 116,    // <video> Defines a video or movie
    wbr        = 117,    // <wbr> Defines a possible line-break
}
